pipeline {
    agent any
    environment {
        EC2_IP = "34.196.168.199"
        DEPLOY_PATH = "/usr/share/nginx/html"
        BUILD_OUTPUT = "dist/car_website/browser"
    }
    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/uzairinayat/car_website.git'
            }
        }
        stage('Install Dependencies') {
            steps {
                bat 'npm ci'
            }
        }
        stage('Build') {
            steps {
                bat 'npm run build'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying to EC2...'
                withCredentials([sshUserPrivateKey(credentialsId: 'ec2-ssh', keyFileVariable: 'SSH_KEY', usernameVariable: 'SSH_USER')]) {
                    bat """
                    ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL -i "%SSH_KEY%" %SSH_USER%@%EC2_IP% "mkdir -p /tmp/deploy_%BUILD_NUMBER%"
                    scp -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL -i "%SSH_KEY%" -r %BUILD_OUTPUT% %SSH_USER%@%EC2_IP%:/tmp/deploy_%BUILD_NUMBER%/
                    ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=NUL -i "%SSH_KEY%" %SSH_USER%@%EC2_IP% "sudo rm -rf %DEPLOY_PATH%/* && sudo cp -r /tmp/deploy_%BUILD_NUMBER%/browser/* %DEPLOY_PATH%/ && sudo rm -rf /tmp/deploy_%BUILD_NUMBER% && sudo systemctl reload nginx"
                    """
                }
            }
        }
    }
    post {
        success {
            emailext(
                to: 'uzairinayat42@gmail.com',
                subject: 'Deployment Success 🚀',
                body: 'car_website deployed successfully on EC2'
            )
        }
        failure {
            emailext(
                to: 'uzairinayat42@gmail.com',
                subject: 'Deployment Failed ❌',
                body: 'Check Jenkins logs'
            )
        }
    }
}
